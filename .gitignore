# dependencies (puppeteer-core is local screenshot tooling only — not needed to deploy)
node_modules/

# secrets & credentials — NEVER commit these
.env
.env.*
credentials.json
token.json

# Claude Code local settings
.claude/settings.local.json

# owner's monthly-note PDFs — local processing source, contain customer PII
# (names, addresses, phones, emails). NEVER commit to GitHub.
*.pdf

# temp / intermediate processing
.tmp/
.DS_Store
.vercel
.env*

# local deliverables with customer PII — keep off GitHub
*.csv

# 年度決算工作資料夾：帳務、客人姓名/地址/電話、税理士往來文件。
# 全資料夾不進版控（本 repo 會 push 到公開 GitHub）。
決算/

# 改檔前留的本機備份（cp <檔> <檔>.bak-YYYYMMDD）。只在本機保留，不進公開版控。
*.bak
*.bak-*

# 税理士決算清單產生器：檔內有客人姓名對地址、電話的補正對照表（行事曆沒帶到的才手動補）。
# 本 repo 是公開的，這支只留本機。
tools/export_tax_ledger.js
